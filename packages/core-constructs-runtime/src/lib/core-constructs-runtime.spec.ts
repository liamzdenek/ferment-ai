import { coreConstructsRuntime } from './core-constructs-runtime.js';

describe('coreConstructsRuntime', () => {
  it('should work', () => {
    expect(coreConstructsRuntime()).toEqual('core-constructs-runtime');
  })
})
