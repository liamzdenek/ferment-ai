# core-constructs-runtime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-constructs-runtime` to build the library.

## Running unit tests

Run `nx test core-constructs-runtime` to execute the unit tests via [Jest](https://jestjs.io).
