# core-constructs-lib

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build core-constructs-lib` to build the library.

## Running unit tests

Run `nx test core-constructs-lib` to execute the unit tests via [Jest](https://jestjs.io).
