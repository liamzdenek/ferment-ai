# preservative

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build preservative` to build the library.

## Running unit tests

Run `nx test preservative` to execute the unit tests via [Jest](https://jestjs.io).
