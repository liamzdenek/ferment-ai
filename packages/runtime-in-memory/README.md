# runtime-in-memory

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build runtime-in-memory` to build the library.

## Running unit tests

Run `nx test runtime-in-memory` to execute the unit tests via [Jest](https://jestjs.io).
